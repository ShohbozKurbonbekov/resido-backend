export enum AgentStatus {
  PENDING = "pending",
  AVAILABLE = "available",
  PAUSED = "paused",
  REJECTED = "rejected",
}
export enum AgentPropertyType {
  NONE = "NONE",
  SALE = "SALE",
  RENT = "RENT",
}
